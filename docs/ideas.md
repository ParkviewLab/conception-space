<!--
SPDX-FileCopyrightText: 2026 Gary Frattarola <garyf@parkviewlab.ai>
SPDX-License-Identifier: CC-BY-4.0
-->


# Importing Nodes with Tools to Help the User Build cluster structure based on target models (sphere, planar, toroid, etc)

IF these nodes each represent an md-note (markdown note) then we'll want a way to import md-notes to create a cluster. A tool that takes a best guess at how to arrange the nodes in space. It would have tools for the user to use to alter this automatically generated locating of the nodes in space. perhaps using frontmatter to group further. perhaps having tools to alter the sapcial arrangement: spherical, planar, galaxy like, toroid, etc (each which appropriate sliders)


# Table of Discovery

Show the beginings of threads to be followed in order to learn certain aspects within a subject.


# Allow choice of containing object for clusters. I want a 3D rectangle, it'll fit in X, Y, an Z to the nodes it contains. this'll be a better, more custom, visual representaion than a sphere. it shows the bounds in each dimention thus giving more visual reference.


# Need to Implement Visual Editting
- maybe node size is related to content size?
- select nodes with dragging select rect to then turn thm into a cluster
- toggle a node to go to it's shape being tanslucent so that one can see a panel within it thzt shows an AI generated (human editable) title and summary for that node (if it's text), visual summary (if it's an image), etc.
- there is what the human placed & connected and then there is an AI overlay showing what the AI added, placed, and connected.